services:
  villa-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: villa-frontend
    ports:
      - "${APP_PORT:-10080}:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:4000/api
